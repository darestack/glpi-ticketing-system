# Copy to .env and replace the example values before running docker compose up.
MARIADB_ROOT_PASSWORD=change_me_root
GLPI_DB_HOST=db
GLPI_DB_PORT=3306
GLPI_DB_NAME=glpi
GLPI_DB_USER=glpi
GLPI_DB_PASSWORD=change_me_glpi
GLPI_CRONTAB_ENABLED=1
